task wrapper(type: Wrapper) {
    gradleVersion = '4.6'
}

allprojects {
    apply plugin: 'maven'

    buildscript {
        repositories {
            mavenLocal()
            jcenter()
        }
    }

    repositories {
        mavenLocal()
        jcenter()
    }
}